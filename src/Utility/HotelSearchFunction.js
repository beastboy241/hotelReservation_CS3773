 /* import axios from 'axios'

export const HotelSearchFunction = temp_fields => {
  return axios.get("http://localhost:3001/api/get/hotels", {
    params: temp_fields
  }).then(response => {
    if (response.status === 200) {
    }
    return response.data
  })
}

*/